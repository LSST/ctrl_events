# -*- python -*-

import lsst.sconsUtils

dependencies = {
    "required": ["utils", "boost", "base", "daf_base", "pex_exceptions", "log4cxx", "log"],
    "buildRequired": ["boost_test"],
}

config = lsst.sconsUtils.Configuration(
    __file__,
    headers=["lsst/ctrl/events.h"],
    hasDoxygenInclude=False,
)
